CONF_DIR=$(pwd)

find . -maxdepth 1 -type f | grep -v "install$" | xargs -i ln -sf $CONF_DIR/{} ~/  
ln -sf $CONF_DIR/.i3 ~/.i3

mkdir -p ~/.config/nvim
ln -sf $CONF_DIR/config/nvim/init.vim ~/.config/nvim/init.vim
